import type { ToolManifest } from "@/lib/types";

export const mdToPdfManifest: ToolManifest = {
  slug: "md-to-pdf",
  title: "Markdown to PDF",
  description:
    "Turn a Markdown file into a PDF with real, selectable text. Choose page size, margins and typeface, then save.",
  category: "content",
};
