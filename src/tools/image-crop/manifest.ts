import type { ToolManifest } from "@/lib/types";

export const imageCropManifest: ToolManifest = {
  slug: "image-crop",
  title: "Image Crop",
  description:
    "Crop a local image — freely, to an aspect ratio, with rounded corners, or to a circle — and download the result.",
  category: "image",
};
