import type { ToolManifest } from "@/lib/types";

export const imageCropManifest: ToolManifest = {
  slug: "image-crop",
  title: "Image Crop",
  description:
    "Crop a local image to a free rectangular region and download the result. Nothing is uploaded, and nothing about the crop is kept.",
  category: "image",
};
